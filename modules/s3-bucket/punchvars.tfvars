// the properties (don't remove the quotation mark = exmaple: region = "eu-central-1")

region                       = "add the region here (e.g: eu-central-1)"

bucket_name                  = "add the bucket name here (e.g: first-bucket)"

bucket_tag                   = "add the bucket tag here (e.g: Name = tf-bucket)"

versioning                   = "set the versioning stauts here (e.g: Enabled)"

sse_encryption_algorithm     = "set the encryption type here (e.g: AES256)"
